class Solution {
    public boolean isMatch(String s, String p) {
      char charsS = s.toCharArray();
      char charsP = p.toCharArray();

      int sPointer = 0;
      int pPointer = 0;
/**
  dp[n] = dp[n-1] 加上charsP[n] 是不是匹配
  switch{
    case '.'
      true
    case '*'
      if(charsP[n-1]=='.'){
        
      }
  }

*/
    }
}